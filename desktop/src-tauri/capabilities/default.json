{
  "$schema": "https://schema.tauri.app/config/2/capabilities",
  "identifier": "default",
  "description": "Default capabilities: window + core events only. No shell, no fsScope, no network beyond the app itself.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:default"]
}
